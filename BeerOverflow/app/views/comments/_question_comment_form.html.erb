<%= form_for [question, comment], :remote => true, class: 'comment_form' do |f| %>
  <%= f.text_area :comment, placeholder: "Got a comment?  Submit it or chug a beer!" %>
  <%= f.hidden_field :user_id, :value => session[:user_id] %>
  <%= f.submit "Submit", class: "question_comment" %>
<% end %>
