<%= form_for([@question, @answer]) do |f| %>
  <%= f.text_area :answer, placeholder: "Got an answer?  Submit it or chug a beer!" %>
  <%= f.hidden_field :user_id, :value => session[:user_id] %>
  <%= f.submit "Submit" %>
<% end %>
